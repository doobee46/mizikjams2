$(‘.results’).append(‘<%= j render @videos %>’);
<% if @videos.next_page %>
$(‘.pagination’).replaceWith(‘<%= j will_paginate @videos %>’);
<% else %>
$(window).off(‘scroll’);
$(‘.pagination’).remove();
<% end %>