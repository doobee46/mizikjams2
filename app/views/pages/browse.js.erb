$('.browse_list ul').append('<%= j render :partial => 'pages/template'%>');
<% if @videos.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @videos %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>